@model IEnumerable<FitnessCenterAspNetMVCApp.Models.Coach>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.coach_surname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coach_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coach_post)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coach_skill)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.coach_surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coach_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coach_post)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coach_skill)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.coach_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.coach_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.coach_id })
        </td>
    </tr>
}

</table>
